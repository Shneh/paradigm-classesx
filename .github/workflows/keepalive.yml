name: keepalive
on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -fsS https://paradigm-backend.onrender.com/health || exit 1
